---
description: Summarize session content to markdown
allowed-tools: Read, Write, Glob, Grep
---

Summarize this session's content into a well-structured markdown document.

Before writing, confirm with the user: what to document and for whom, where to save it (directory and filename), and whether to append to an existing file or create a new one.

Formatting: use heading levels appropriately, prefer lists over long paragraphs, wrap code in code blocks, and use relative paths for links where possible.

## Additional Instructions

$ARGUMENTS
